import 'package:flutter/material.dart';

class InvestorBio extends StatelessWidget {
  const InvestorBio({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
